services:
  bicep:
    image: guitarrapc/docker-bicep:0.4.1008
    entrypoint: /bin/bash
    tty: true
    stdin_open: true
    volumes:
      - .:/bicep
      - azure:/root/.azure

volumes:
  azure:
    driver: local
